{
  "name": "@htmelt/unocss",
  "version": "0.1.2",
  "main": "dist/index.mjs",
  "types": "dist/index.d.mts",
  "scripts": {
    "dev": "tsc -p ./ --watch --sourceMap",
    "build": "rimraf dist && tsc -p ./",
    "prepublishOnly": "rimraf dist && tsc -p ./"
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "@htmelt/plugin": "workspace:^0.1.2",
    "@unocss/config": "^0.50.8",
    "@unocss/core": "^0.50.8"
  }
}
