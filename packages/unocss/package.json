{
  "name": "@htmelt/unocss",
  "version": "0.2.1",
  "main": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "tsup src/index.mts --format esm --dts --watch --sourcemap --declarationMap",
    "build": "tsup src/index.mts --format esm --dts --clean",
    "prepublishOnly": "tsup src/index.mts --format esm --dts --clean"
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "@htmelt/plugin": "workspace:^0.2.0",
    "@unocss/config": "^0.50.8",
    "@unocss/core": "^0.50.8"
  }
}
