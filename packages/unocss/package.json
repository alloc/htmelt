{
  "name": "@htmelt/unocss",
  "version": "0.1.1",
  "main": "dist/index.mjs",
  "types": "dist/index.d.mts",
  "scripts": {
    "dev": "tsc -p ./ --watch --sourceMap",
    "build": "rimraf dist && tsc -p ./",
    "prepublishOnly": "rimraf dist && tsc -p ./"
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "@htmelt/plugin": "^0.1.0",
    "@unocss/config": "^0.50.6",
    "@unocss/core": "^0.50.6"
  }
}
