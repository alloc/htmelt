{
  "name": "@htmelt/unocss",
  "version": "0.3.2",
  "main": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "tsup src/index.mts --format esm --dts --watch --sourcemap",
    "build": "tsup src/index.mts --format esm --dts --clean",
    "prepublishOnly": "tsup src/index.mts --format esm --dts --clean"
  },
  "files": [
    "dist",
    "env.d.ts"
  ],
  "dependencies": {
    "@htmelt/plugin": "workspace:^"
  },
  "peerDependencies": {
    "@unocss/core": "*"
  },
  "devDependencies": {
    "@unocss/core": "^0.58.9"
  }
}
